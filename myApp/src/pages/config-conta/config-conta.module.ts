import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ConfigContaPage } from './config-conta';

@NgModule({
  declarations: [
    ConfigContaPage,
  ],
  imports: [
    IonicPageModule.forChild(ConfigContaPage),
  ],
})
export class ConfigContaPageModule {}
